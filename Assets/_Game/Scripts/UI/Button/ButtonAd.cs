using UnityEngine;

public class ButtonAd : MonoBehaviour
{
/*    [Header("Data")]
    public AdType adType;
    public bool isOptional;

    [Header("Event Action")]
    public UnityEvent<bool> onComplateAd;
    void Start()
    {
        GetComponent<Button>().onClick.AddListener(() =>
        {
            switch (adType)
            {
                case AdType.None:
                    break;
                case AdType.Interstitial:
                    AdsManager.instance.InterstitialShowAd(isOptional, onComplateAd);
                    break;
                case AdType.Rewarded:
                    //AdsManager.instance.InterstitialShowAd();
                    break;
                default:
                    break;
            }
        });
    }*/

}
